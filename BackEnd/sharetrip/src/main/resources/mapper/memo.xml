<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.orj.sharetrip.memo.model.mapper.MemoMapper">

	<resultMap type="memoDto" id="memo">
		<result column="user_id" property="userId"/>
		<result column="content" property="content"/>
		<result column="regist_time" property="registTime"/>
	</resultMap>

	<insert id="registAttrMemo" parameterType="map">
		insert into attraction_memo (content_id, user_id, content, regist_time)
		values (#{contentId}, #{userId}, #{content}, now())
	</insert>
	
	<delete id="deletetAttrMemo" parameterType="map">
		delete from attraction_memo
		where content_id = #{contentId} and user_id = #{userId}
	</delete>
	
</mapper>